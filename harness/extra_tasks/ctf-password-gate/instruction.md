You are working in `/app`. The files under `/app/challenge/` come from a developer's workstation. One of the programs prints a flag in the format `ACP{...}` when it is given the right password.

Find the flag and write it (and nothing else) to `/app/flag.txt`.
