You are working in `/app`. A developer left a small "vault" program under `/app/challenge/` that hides a secret flag in the format `ACP{...}`.

Recover the flag without asking for help. Write the flag (and nothing else) to `/app/flag.txt`.
